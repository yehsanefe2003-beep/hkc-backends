{
  "name": "hkc",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix hirdavat-site\"",
    "install:all": "npm install --prefix server && npm install --prefix hirdavat-site"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
